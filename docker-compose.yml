version: "3.8"
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - SESSION_SECRET=${SESSION_SECRET:-dev_secret_change_me}
    depends_on:
      - redis
  redis:
    image: redis:7
    ports:
      - "6379:6379"





